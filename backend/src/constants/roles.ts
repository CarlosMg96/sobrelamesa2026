export enum Role {
    MASTER = 1,    // SUPER-ADMIN o MASTER
    ADMIN = 2,     // ADMIN
    SALES = 3,     // VENTAS → SALES
    WAREHOUSE = 4, // ALMACEN → WAREHOUSE
    STAFF = 5,     // STAFF (igual)
}
